apiVersion: v1
kind: Service
metadata:
  name: image-svc
  namespace: project
spec:
  type: ClusterIP
  selector:
    app: image-service
  ports:
    - protocol: TCP
      port: 1234
      targetPort: 3002
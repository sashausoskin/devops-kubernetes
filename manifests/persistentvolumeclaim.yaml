apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pingpong-claim
  namespace: exercises
spec:
  storageClassName: pingpong-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi